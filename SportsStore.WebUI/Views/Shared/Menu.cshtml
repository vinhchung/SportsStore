@model IEnumerable<string>

@Html.ActionLink("Home", "List", "Product")

@foreach (string link in @Model)
{
    @Html.RouteLink(link, new { controller = "Product", action = "List", category = link.ToLower(), page = 1 }, new { @class = link.ToLower()==ViewBag.SelectedCategory ? "selected" : null})
}